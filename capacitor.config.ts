import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'react-18-test',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
